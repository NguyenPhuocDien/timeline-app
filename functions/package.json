{
  "name": "timeline-focus-functions",
  "description": "Cloud Functions backend for two-way Google Calendar sync",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions --config ../apps/web/firebase.json",
    "deploy": "firebase deploy --only functions --config ../apps/web/firebase.json"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {}
}
