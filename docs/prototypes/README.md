# Prototypes

This folder stores design explorations and imported demos that are not part of
the production web app.

- `claude-demo/`: archived Claude-generated prototype kept for visual reference.

Production runtime files stay in `apps/web` so Firebase and Vercel deployments
remain predictable.
