# Working Notes

- This folder is the Next.js/Supabase product.
- It is independent from the Firebase product in `../legacy-pwa`.
- Prefer changes that keep the beta deployable before broader feature expansion.
- Do not assume the legacy production domain serves this application.
